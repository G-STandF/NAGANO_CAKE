<div class="mt-5" style="font-size :20px;" >
  ジャンルから探す
</div>
  <table>
    <tr>
      <td>
        <%= link_to "全商品を表示", products_path, style: "text-decoration:none;" %>
      </td>
    </tr>
    <% genres.each do |genre| %>
      <tr>
        <td>
          <%= link_to genre.name, genres_path(genre.id), style: "text-decoration:none;"%>
          </td>
      </tr>
    <% end %>
  </table>