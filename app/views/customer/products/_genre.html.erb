<div class="mt-5" style="font-size :20px;" >
  ジャンルから探す
</div>
  <table>
    <tr>
      <td>
        <%= link_to "全商品を表示", customer_products_path, style: "text-decoration:none;" %>
      </td>
    </tr>
    <% genres.each do |genre| %>
      <tr>
        <td>
          <%= link_to genre.name, customer_genre_path(genre.id), style: "text-decoration:none;"%>
          </td>
      </tr>
    <% end %>
  </table>