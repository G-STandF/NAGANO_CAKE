  <table class="table table-bordered mt-4">
    <thead class="text-center">
      <tr>
        <th>ジャンルから探す</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>
          <%= link_to "全商品を表示", products_path, class: "text-dark" %>
        </td>
      </tr>
      <% genres.each do |genre| %>
        <tr>
          <td>
            <%= link_to genre.name, genre_path(genre.id), class: "text-dark" %>
            </td>
        </tr>
      <% end %>
    </tbody>
  </table>