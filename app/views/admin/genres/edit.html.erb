<h2>ジャンル編集</h2>
<%= form_with model: [:admin,@genre] do |f| %>
<table>
  <tbody>
    <tr>
      <td>ジャンル名</td>
      <td><%= f.text_field :name %></td>
      <td><%= f.submit "変更を保存" %></td>
    </tr>
  </tbody>
</table>
<%end%>